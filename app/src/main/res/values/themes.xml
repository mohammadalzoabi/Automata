<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Automata" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item> //don't change
        <item name="colorPrimaryVariant">@color/black</item> //don't change
        <item name="colorOnPrimary">@color/black</item> //don't change
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">#FFFDFA</item>
        <item name="android:colorSecondary">@color/lightRed</item>
        <item name="android:colorAccent">@color/bannerWhite</item>
    </style>

</resources>