<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Automata" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item> //don't change
        <item name="colorPrimaryVariant">@color/black</item> //don't change
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/matteBlack</item>
        <item name="colorSecondaryVariant">@color/matteBlack</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">#424242</item>
        <item name="android:colorSecondary">@color/lightRed</item>
        <item name="android:colorAccent">@color/bannerBlack</item>
    </style>
</resources>